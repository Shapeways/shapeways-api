// Generated by CoffeeScript 1.4.0

/* Object representing a model file on Shapeways
*/


(function() {
  var Auth, Models, auth, cfg, fs;

  cfg = require('../cfg/config.js');

  Auth = (require('./auth.js')).Auth;

  fs = require('fs');

  auth = new Auth;

  exports.Models = Models = (function() {

    function Models() {}

    Models.prototype.getModels = function(oauth_access_token, oauth_access_token_secret, callback) {
      return auth.oa.getProtectedResource("http://api." + cfg.API_SERVER + "/models/" + cfg.API_VERSION, 'GET', oauth_access_token, oauth_access_token_secret, function(error, data, response) {
        if (error) {
          console.log('error :' + JSON.stringify(error));
        }
        return callback(data);
      });
    };

    Models.prototype.getModel = function(id, oauth_access_token, oauth_access_token_secret, callback) {
      return auth.oa.getProtectedResource("http://api." + cfg.API_SERVER + "/models/" + id + "/" + cfg.API_VERSION, 'GET', oauth_access_token, oauth_access_token_secret, function(error, data, response) {
        if (error) {
          console.log('error :' + JSON.stringify(error));
        }
        return callback(data);
      });
    };

    Models.prototype.putModel = function(file, oauth_access_token, oauth_access_token_secret, callback) {
      var model_upload;
      return model_upload = fs.readFile(file.path, function(err, fileData) {
        var upload;
        fileData = encodeURIComponent(fileData.toString('base64'));
        upload = JSON.stringify({
          file: fileData,
          fileName: file.name,
          hasRightsToModel: 1,
          acceptTermsAndConditions: 1
        });
        return auth.oa.post("http://api." + cfg.API_SERVER + "/models/" + cfg.API_VERSION, oauth_access_token, oauth_access_token_secret, upload, function(error, data, response) {
          if (error) {
            console.log('error: ' + JSON.stringify(error));
            return console.log(error);
          } else {
            return callback(data);
          }
        });
      });
    };

    return Models;

  })();

}).call(this);
